name: Video Editor Workflow

on:
  workflow_dispatch: # يسمح لنا بتشغيله يدوياً

jobs:
  create-video:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          python -m pip --version
          python -m pip install --upgrade pip
          pip install requests
          pip install google-api-python-client google-auth-httplib2 google-auth-oauthlib
          pip install moviepy
          pip install Pillow

      - name: Run Video Editor Script
        env:
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }} # سنقوم بإضافة هذا المفتاح لاحقاً
          GOOGLE_DRIVE_FOLDER_ID: ${{ secrets.GOOGLE_DRIVE_FOLDER_ID }} # وهذا أيضاً
        run: python video_editor.py

      - name: Upload Video Artifact
        uses: actions/upload-artifact@v3
        with:
          name: final-video
          path: final_video.mp4
